const { createClient } = require('@supabase/supabase-js'); require('dotenv').config({ path: '.env.local' }); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); async function run() { const { error } = await supabase.from('broadcast_schedule').delete().gte('start_time', '2026-05-28T16:30:00Z').lt('start_time', '2026-05-28T17:30:00Z'); console.log('Wiped 22:00 IST block', error || 'Success'); } run();
