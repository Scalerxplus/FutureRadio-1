const { createClient } = require('@supabase/supabase-js'); require('dotenv').config({ path: '.env.local' }); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); async function run() { const { data } = await supabase.from('broadcast_schedule').select('id, start_time, duration_ms, element_type, metadata').gte('start_time', '2026-05-28T16:30:00Z').lt('start_time', '2026-05-28T17:30:00Z').order('start_time'); console.log(JSON.stringify(data, null, 2)); } run();
