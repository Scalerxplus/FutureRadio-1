const { createClient } = require('@supabase/supabase-js'); require('dotenv').config({ path: '.env.local' }); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); async function run() { const now = new Date(); const istTimeMs = now.getTime() + 5.5 * 60 * 60 * 1000; const istDate = new Date(istTimeMs); const startIST = new Date(istDate); startIST.setUTCHours(0, 0, 0, 0); const startOfTodayISO = new Date(startIST.getTime() - 5.5 * 60 * 60 * 1000).toISOString(); const endIST = new Date(istDate); endIST.setUTCHours(23, 59, 59, 999); const endOfTodayISO = new Date(endIST.getTime() - 5.5 * 60 * 60 * 1000).toISOString(); const { data } = await supabase.from('broadcast_schedule').select('*').eq('city_id', 'raipur').gte('start_time', startOfTodayISO).lte('start_time', endOfTodayISO).order('start_time', { ascending: true }).limit(5000); console.log('Total items fetched:', data.length); } run();
