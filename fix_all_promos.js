const { createClient } = require('@supabase/supabase-js'); require('dotenv').config({ path: '.env.local' }); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); async function run() { const { data } = await supabase.from('broadcast_schedule').select('*').like('media_url', '%3_Station_Promo%'); const newPromos = [ { url: '/local_audio_vault/roots/3_Station_Promo/Future Radio Promo - Cross Platform.mp3', title: 'Future Radio Promo - Cross Platform' }, { url: '/local_audio_vault/roots/3_Station_Promo/Music of India\'s Heartland.mp3', title: 'Music of India\'s Heartland' }, { url: '/local_audio_vault/roots/3_Station_Promo/Creator Invite Promo.mp3', title: 'Creator Invite Promo' }, { url: '/local_audio_vault/roots/3_Station_Promo/Station Promo - Roots.mp3', title: 'Station Promo - Roots' } ]; let i = 0; for (const row of data) { if (row.media_url && (row.media_url.includes('Bagheli') || row.media_url.includes('Baghell'))) { const newPromo = newPromos[i % newPromos.length]; const newMetadata = { ...row.metadata, title: newPromo.title }; await supabase.from('broadcast_schedule').update({ media_url: newPromo.url, metadata: newMetadata }).eq('id', row.id); i++; } } console.log('Updated ' + i + ' promo rows completely'); } run();
